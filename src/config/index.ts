import type * as Base from '../typings'

/**
 * 存储类型
 */
export const storageTypes: Base.storageType[] = ['session', 'local']

/**
 * 存储全类型
 */
export const storageFullTypes: Base.storageFullType[] = ['sessionStorage', 'localStorage']
